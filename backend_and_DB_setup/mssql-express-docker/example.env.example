# ============================================
# Environment Variables for Habit Tracker Database
# ============================================

# SQL Server SA credentials (for initial setup only)
SA_PASSWORD=YourStrong!Passw0rd123!
ACCEPT_EULA=Y

# Application database credentials (recommended for app connections)
DB_SERVER=localhost
DB_PORT=1433
DB_NAME=HabitTrackerDB
DB_USER=habit_app_user
DB_PASSWORD=HabitApp!Secure2025

# Connection string for application use
DB_CONNECTION_STRING=Server=localhost,1433;Database=HabitTrackerDB;User Id=habit_app_user;Password=HabitApp!Secure2025;TrustServerCertificate=true;

# Security Notes:
# - Change these passwords before production use
# - Never commit real credentials to version control
# - Consider using Azure Key Vault or similar for production
# - The SA account should only be used for administrative tasks